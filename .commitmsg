feat: frontend error boundaries, 404 page, and loading state

- Add app-level error.tsx error boundary with retry button
- Add custom not-found.tsx 404 page
- Add loading.tsx with animated spinner
- Add route-level error boundaries for dashboard and reports
- All 170 backend tests pass
