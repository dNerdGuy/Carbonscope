feat: DB indexes, FK enforcement, and soft delete

- Add composite indexes on (company_id, year) for data_uploads and emission_reports
- Add single-column indexes on company_id for DataUpload and EmissionReport
- Enable SQLite foreign key enforcement via PRAGMA foreign_keys=ON
- Add deleted_at column to DataUpload and EmissionReport for soft delete
- Convert DELETE endpoints to soft-delete (set deleted_at timestamp)
- Filter soft-deleted records from all list/get/count/dashboard queries
- All 167 tests pass
